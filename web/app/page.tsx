import { PredictorPage } from "./predictor/PredictorPage";

/** "/" is the predictor. See PredictorPage for why the tile grid that used
 *  to live here went away. */
export default function Home() {
  return <PredictorPage />;
}
