import { LabPage } from "./LabPage";

export const metadata = {
  title: "Component lab — Predictorous",
  description: "Every 3D piece on its own, with synthetic data.",
};

export default function Page() {
  return <LabPage />;
}
